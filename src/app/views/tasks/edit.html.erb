<div class="card">
  <h1>タスク編集</h1>
  <p class="muted-text">最新の状況に合わせてタスク情報を更新しましょう。</p>

  <%= render "form" %>

  <div class="actions">
    <%= link_to "詳細に戻る", @task, class: "btn btn-secondary" %>
    <%= link_to "一覧", tasks_path, class: "btn btn-secondary" %>
  </div>
</div>
