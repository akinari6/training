<div class="card">
  <h1>タスク一覧</h1>

  <p class="muted-text">重要度や期限で整理されたタスクの一覧です。完了状況を素早く切り替えられます。</p>

  <div class="actions">
    <%= link_to "新規タスク", new_task_path, class: "btn btn-primary" %>
  </div>

  <table>
    <thead>
      <tr>
        <th>タイトル</th>
        <th>期限</th>
        <th>優先度</th>
        <th>状態</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @tasks.each do |task| %>
        <tr class="<%= task.completed? ? 'completed' : '' %>">
          <td><%= link_to task.title, task %></td>
          <td><%= l(task.due_at, format: :short) if task.due_at %></td>
          <td><%= task.priority %></td>
          <td><%= task.completed? ? '完了' : '未完了' %></td>
          <td>
            <div class="task-actions">
              <% if task.completed? %>
                <%= button_to '再開', reopen_task_path(task), method: :patch, class: 'btn btn-secondary' %>
              <% else %>
                <%= button_to '完了', complete_task_path(task), method: :patch, class: 'btn btn-secondary' %>
              <% end %>
              <%= link_to '編集', edit_task_path(task), class: 'btn btn-secondary' %>
              <%= button_to '削除', task, method: :delete, class: 'btn btn-secondary', form: { data: { turbo_confirm: '本当に削除しますか？' } } %>
            </div>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
